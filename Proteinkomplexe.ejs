<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="box">
        <h1>Proteinkomplexe</h1>
    </div>
    <p>
        <form id="searchForm" action="/search" method="get">
            <input type="text" id="searchInput" name="q" placeholder="Suchbegriff eingeben..." value="<%= query %>">
            <button type="submit">Suchen</button>
        </form>
        <div id="results"></div>
        <div class="container">
            <table id="complexinfo">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Synonym</th>
                        <th>Organismus</th>
                        <th>Zelllinie</th>
                    </tr>
                </thead>
                <tbody id="table-body">
                    <% if (results.length > 0) { %>
                        <% results.forEach(function(item) { %>
                            <tr>
                                <td><%= item.complexid %></td>
                                <td><%= item.name %></td>
                                <td><%= item.synonym %></td>
                                <td><%= item.organismus %></td>
                                <td><%= item.cell_line %></td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="5">No results found.</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
            <div id="pagination" class="pagination button">
                <% if (totalPages > 1) { %>
                    <% if (page > 1) { %>
                        <a href="/search?q=<%= query %>&page=<%= page - 1 %>">&laquo; Previous</a>
                    <% } %>
                    <span> Page <%= page %> of <%= totalPages %> </span>
                    <% if (page < totalPages) { %>
                        <a href="/search?q=<%= query %>&page=<%= page + 1 %>">Next &raquo;</a>
                    <% } %>
                <% } %>
            </div>
        </div>
    </p>
</body>
</html>